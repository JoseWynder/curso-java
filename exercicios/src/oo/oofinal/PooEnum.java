package oo.oofinal;

public enum PooEnum {

	// Enumeração(Enum) é usada quando você tem uma quantidade definida de possibilidades.
	// Enum também defini um tipo, uma estrutura de dados.
	
	NORTE, LESTE, SUL, OESTE;
}
