package br.com.cod3r.cm.campominadofinal;

public class CampoMinadoDefinitivo {

	
	// CONCEITOS TRABALHADOS NO PROJETO
	// que não foram trabalhados anteriormente.
	
	
	/*
	 * Quando falamos de testes, embutido no eclipse já vem uma biblioteca chamada JUnit.
	 * Que é a biblioteca de teste unitário mais famosa no mundo do java.
	 * 
	 * Criar um novo Java Project.(Projeto java)
	 * 
	 * Nomenclatura de pacotes:
	 * 
	 * Para evitar possíveis conflitos com o nome dos pacotes e classes,
	 *  normalmente os projetos começam a nomenclatura de projetos com o endereço do
	 *  domínio da empresa, ao contrário.
	 *  
	 *  Ex: br.com.cod3r.campoMinado
	 *  
	 * Padrão de projeto Model View Controller (MVC). Não foi realmente seguido no projeto,
	 *  mas foi usando como referência.
	 *  
	 *  
	 * Configurar o projeto usando o JUnit, que é um framework para
	 *  aplicar testes dentro de um projeto java.
	 *  
	 *  Os testes são feitos em pacotes diferentes do projeto já que o testes só seram
	 *   executados durante o processo de desenvolvimento.
	 *   
	 *   Criar uma Source Folder. (Pasta de código fonte)
	 *   
	 * Inicialmente o pacote só tem uma biblioteca, a "JRE System Library".
	 * Dentro dela temos toda a API do java.
	 * 
	 * É preciso adicionar uma nova biblioteca que já está disponível dentro do eclipse.
	 * Não é necessário baixar nada da internet.
	 * 
	 * Botão direito no projeto, em "Properties",
	 *  selecionar "Java Build Path" e ir em "Libraries".
	 *  Selecionar "Classpath", clicar em "Add Library" e selecionar "JUnit".
	 *  Escolher JUnit 5, clicar em "Finish" e Aplicar as alterações.
	 *  
	 * Criar um New JUnit test Case dentro do pacote test.
	 * 
	 * Definir o nome do pacote de br.com.cod3r.campoMinado
	 *  e o nome da classe de AlgumaCoisaTeste.
	 * 
	 */
	
}
